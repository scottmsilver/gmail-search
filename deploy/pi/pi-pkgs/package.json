{
  "name": "pi-pkgs",
  "private": true,
  "description": "Pinned runtime deps for pi-mcp-adapter, installed into /opt/pi-pkgs inside the pi-sandbox image. Lockfile committed so the image build is reproducible instead of re-resolving npm's dependency graph on every build.",
  "dependencies": {
    "pi-mcp-adapter": "2.32.1",
    "typebox": "1.3.25",
    "@earendil-works/pi-ai": "0.84.4",
    "@earendil-works/pi-tui": "0.84.4"
  }
}
