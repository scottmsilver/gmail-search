"""Deep-analysis agent service.

An opt-in multi-agent flow that sits next to the Next.js chat route.
See `docs/DEEP_ANALYSIS_AGENT.md` for architecture + build phases.

Phase 1 (this module): service skeleton, session + event persistence,
stub streaming endpoint. Real sub-agents land in subsequent phases.
"""
